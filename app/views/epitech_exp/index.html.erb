<h1>Générez votre planning pour le reste de la semaine et recevez le par mail</h1>
<%= simple_form_for :meal_plan_infos,
                    url: epiexp_create_meal_plan_path,
                    :method => :post,
                    html: {
                        class: "uk-grid-small uk-grid uk-padding search_form"
                    },
                    defaults: {
                        wrapper_class: "uk-width-1-1",
                        required: false
                    } do |f| %>
    <%= f.input :email, :placeholder => 'Email', as: :string, input_html: { value: @search_query }, wrapper_class: "uk-width-2-3@s", label: false %>
    <div class="uk-width-1-3@s">
        <%= f.button :submit, 'Générer un planing', class: "uk-width-1-1" %>
    </div>
<% end %>